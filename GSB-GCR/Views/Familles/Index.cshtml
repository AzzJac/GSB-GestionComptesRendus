@model IEnumerable<GSB_GCR.Models.Famille>

@{
    ViewData["Title"] = "Index";
}

<h1>Familles</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Nouvelle famille</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.Label("Familles")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FamLibelle)
            </td>
            <td>
                @* <a asp-action="Edit" asp-route-id="@item.FamCode">Edit</a> | *@
                    <a asp-action="Details" asp-route-id="@item.FamCode">Détails</a> @*|
                <a asp-action="Delete" asp-route-id="@item.FamCode">Delete</a> *@
            </td>
        </tr>
}
    </tbody>
</table>
